import React,{Component} from 'react';
import ReactDOM from 'react-dom';
// import {db} from "../firebase";

const resAddImage = (props) =>{
    
        return(
       
            <img className="img col-sm-12 col-md-12 col-lg-12 col-xl-4 center"
                 src={props.image}/>
        
        )
        
        }
        export default resAddImage;

